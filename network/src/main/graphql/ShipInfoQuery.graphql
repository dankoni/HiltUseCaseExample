query ShipInfo($starshipId: ID!) {
    starship(id: $starshipId) {
        id
        crew
        length
        manufacturers
        model
        name
        starshipClass
        passengers
    }
}